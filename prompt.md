We're going to develop a vulnerability managment 
   web app and API using Ruby on Rails (RoR). We   
  will use the datahase postgresql and for style   
  tailwind. The name of the app is "hunter". The   
  purpose is to build a web app ui for bug bounty  
  hunting vulnerability managment so we do not     
  need as many features as a robust vulnerability  
  managment service. The API will be used to       
  connect to Mongodb that holds the result of      
  vulnerabilities. Postgresql will be used for     
  users and settings related to them and config. I 
   made a docker-compose.yaml and Dockerfile but   
  they are copy-pasted from an old project named   
  "scope" but we want similar structure but        
  adapted to our vulnerability managment app       
  "hunter". Can you setup Ruby on Rails and make   
  the compose and Dockerfile work? We need a debug 
   compose so we can get live updates as well from 
   within the web folder where we will write the   
  Web app and API.