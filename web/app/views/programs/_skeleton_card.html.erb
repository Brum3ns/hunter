<%# Placeholder card with shimmer blocks. Used as the loading state the
    infinite-scroll controller injects before the next page resolves. %>
<article class="overflow-hidden rounded-xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-[#111315]">
  <div class="h-28 animate-pulse bg-zinc-100 dark:bg-zinc-800"></div>
  <div class="space-y-4 p-4">
    <div class="space-y-2">
      <div class="h-3 w-2/3 animate-pulse rounded bg-zinc-100 dark:bg-zinc-800"></div>
      <div class="h-2.5 w-1/3 animate-pulse rounded bg-zinc-100 dark:bg-zinc-800"></div>
    </div>
    <div class="grid grid-cols-3 gap-2">
      <div class="h-12 animate-pulse rounded-md bg-zinc-100 dark:bg-zinc-800"></div>
      <div class="h-12 animate-pulse rounded-md bg-zinc-100 dark:bg-zinc-800"></div>
      <div class="h-12 animate-pulse rounded-md bg-zinc-100 dark:bg-zinc-800"></div>
    </div>
    <div class="h-5 animate-pulse rounded bg-zinc-100 dark:bg-zinc-800"></div>
  </div>
</article>
